
import Button from 'react-bootstrap/Button';

function Btn(props) {

  return (
    <>
      {/* <Button variant="primary">Primary</Button>{' '} */}
      {/* <Button onClick={() => props.editItem(props.index)}  variant="success">{props.edit}</Button>
      <Button onClick={() => props.deleteItem(props.index)} variant="danger">{props.delete}</Button> */}
    </>
  );
}

export default Btn;